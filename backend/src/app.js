//Set up your express app here. This is where you can import and use middleware, set up your routes, and configure your app to listen for requests. 
//You can also import other modules and use them in your app as needed.
import express from "express";//import express

const app = express();//this creates an express app.

export default app;